const { authenticate, db, env, googleClient, id, admin } = require('../_lib/server');
module.exports = async (req,res)=>{try{const actor=await authenticate(req);const state=id();await db().collection('oauthStates').doc(state).set({uid:actor.uid,createdAt:admin.firestore.FieldValue.serverTimestamp()});const client=googleClient();const url=client.generateAuthUrl({access_type:'offline',prompt:'consent',scope:['https://www.googleapis.com/auth/spreadsheets','https://www.googleapis.com/auth/drive.readonly'],state});res.status(200).json({url});}catch(e){res.status(e.status||500).json({error:e.message,code:e.code||'SERVER_ERROR'});}};
